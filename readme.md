# Система бронирования (C#)

Тестовое задание для собеседования

## Учет времени

На решение задачи мне потребовалось:

| задача                                   | время, час |
|------------------------------------------|------------|
| Вникнуть в ТЗ                            | 0.1        |
| Создать проекты                          | 4.0        |
| Сформировать модель                      | 5.1        |
| Формирование образцов модели домена      | 11.3       |
| Тестирование модели домена               | 2.2        |
| Разработки службы доступа к данным       | 24.5       |
| Конфигурация Ef Core доступа к данным    | 18.8       |
| Тестирование хранения в БД SQLite        | 6.1        |
| Механизм обработки кодов ошибок          | 8.4        |
| Интеграция библиотеки Validot            | 14.5       |
| Механизм обработки бизнес логики         | 48.9       |
| Механизм обработки событий               | 43.2       |
| Тестирование механизма бизнес-логики     | 17.7       |
| Бизнес-логика управления номерным фондом | 12.6       |
| Бизнес-логика управления персонами       | 6.1        |
| Бизнес-логика за(вы)селения              | 4.6        |
| Оформление и документация                | 26.4       |

Всего: __ часов.

## Инструкция для запуска

Результат сборки проекта сохраняется в каталоге %temp%\gmafffff\gmafffff.training.hotel

## Что сделано не так

1. С типами идентификаторов у сущностей нужно было определиться сразу.
   К моменту создания DTO тип Id должен быть известен.
2. Английский язык. Проект намного лучше смотрелся бы на русском.

## Стартовый набор

Чтобы упростить решение типовых задач обработки ошибок и CRUD, создан проект `gmafffff.starterKit`
Подробности [здесь](./gmafffff.starterKit/docs/index.md)

## Дополнительные приемы, которые можно заимствовать

1. Настройка образцов в Autofixture.
2. Использование в тестах XUnit `IClassFixture`, `ITestOutputHelper`.
3. Логирование в файл и на консоль с помощью [Serilog](https://serilog.net) и его настройка через файл конфигурации и
   код.
4. Исключить замусоривание папки проекта промежуточными файлами с помощью файла `Directory.Build.props`.
5. Использование [соглашения о комитах](https://www.conventionalcommits.org/ru/v1.0.0/).

## Оригинальное задание

Реализовать систему администрирования отеля.
Требования к функционалу:

1) Система должна представлять собой web-приложение, имеющее страницу авторизации и страницу администрирования.
2) В БД системы должна иметься таблица пользователей, содержащая логины и хэши паролей.
3) Страница администрирования должна содержать таблицу номеров отеля с возможностью поиска и сортировки по различным
   полям.
4) Должна иметься операция заселения в номер при которой создается новая запись о посетителе с датой въезда.
5) Должна иметься операция выезда из номера, при которой номер освобождается и подсчитывается оплата в зависимости от
   типа номера, количества мест и времени.

Требования к предметной области:
В отеле имеется определенное количество номеров. У каждого номера имеется: вместимость — количество человек, которое
может в нем проживать, тип - стандартный, полу люкс и люкс, состояние - занят или не занят и т.п.
При въезде в номер создается запись в таблице посетитель, которая содержит ФИО въезжающего, дату въезда и дату выезда.
Для системы авторизации должна иметься таблица пользователей, содержащая логин и хэш пароля.
Прочий дополнительный функционал создается на усмотрение.

Требования к технической стороне:
Система должна быть реализована с использованием ASP.NET Core, для системы авторизации необходимо использовать её, для
работы с БД ORM Entity Framework Core. Желательно проектировать api с учетом REST. Для документации API использовать
swagger ui. Фронтенд на усмотрение. Дополнительный функционал приветствуется.

## Лицензия

MIT
